---
phase: 20-codecov-badges
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - COV-01
  - COV-02
  - BADGE-01
  - BADGE-02
user_setup:
  - service: codecov
    why: "Coverage reporting to Codecov dashboard"
    env_vars:
      - name: CODECOV_TOKEN
        source: "Codecov Dashboard → Settings → YAML → token"
    dashboard_config:
      - task: "Add repository to Codecov"
        location: "https://app.codecov.io → Add new repository"
must_haves:
  truths:
    - "CI uploads coverage to Codecov on every push/PR"
    - "README displays current NPM version"
    - "README displays current Codecov coverage percentage"
    - "TypeDoc docs (using README.md) display working badges"
  artifacts:
    - path: "README.md"
      provides: "Badges for NPM version and Codecov coverage"
      contains: "![npm version]"
    - path: ".github/workflows/test.yml"
      provides: "Codecov upload action"
      contains: "codecov/codecov-action"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "Codecov API"
      via: "codecov/codecov-action@v4"
      pattern: "codecov/codecov-action@v4"
    - from: "README.md"
      to: "shields.io"
      via: "npm badge URL"
      pattern: "img.shields.io/npm/v"
---

<objective>
Verify Codecov integration is working and ensure badge URLs are correct for displaying coverage and version information.

Purpose: Users can see test coverage trends and current package version at a glance
Output: Verified/fixed badges and Codecov configuration
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Verify and fix badge URLs in README.md</name>
  <files>README.md</files>
  <action>
  Verify the badges in README.md have correct URLs. The badges are at lines 5-11.

  **Expected badge configuration:**

  1. **Codecov Coverage Badge (line 5):**
     - Badge URL: `https://codecov.io/gh/KrisGray/opencarl/branch/main/graph/badge.svg`
     - Link URL: `https://codecov.io/gh/KrisGray/opencarl`
     - Alt text: "Coverage"
     - If URL is wrong, fix it to use the format above

  2. **NPM Version Badge (line 9):**
     - Badge URL: `https://img.shields.io/npm/v/@krisgray/opencarl?style=for-the-badge&logo=npm&logoColor=white&color=CB3837`
     - Link URL: `https://www.npmjs.com/package/@krisgray/opencarl`
     - If URL is wrong, fix it

  3. **Verify badges are in the header section** (inside `<div align="center">` block)

  **Do NOT change:**
  - Badge styling parameters
  - Other badges (License, GitHub stars)
  - Any content outside the badge section
  </action>
  <verify>
    <automated>grep -q 'codecov.io/gh/KrisGray/opencarl' README.md && grep -q 'img.shields.io/npm/v/@krisgray/opencarl' README.md</automated>
    <manual>Verify badge URLs point to correct Codecov and NPM endpoints</manual>
  </verify>
  <done>README.md contains correct Codecov badge URL and NPM badge URL</done>
</task>

<task type="auto">
  <name>Verify Codecov upload in test.yml workflow</name>
  <files>.github/workflows/test.yml</files>
  <action>
  Verify the Codecov upload step in test.yml is correctly configured.

  **Expected configuration (lines 52-58):**
  ```yaml
  - name: Upload coverage to Codecov
    uses: codecov/codecov-action@v4
    if: always()
    with:
      files: ./coverage/lcov.info
      fail_ci_if_error: false
      token: ${{ secrets.CODECOV_TOKEN }}
  ```

  **Verification checks:**
  1. Uses `codecov/codecov-action@v4` (not v3)
  2. `files` points to `./coverage/lcov.info`
  3. `token` references `${{ secrets.CODECOV_TOKEN }}`
  4. `fail_ci_if_error` is `false` (soft failure, doesn't block CI)

  If any of these are missing or incorrect, fix them.
  </action>
  <verify>
    <automated>grep -q 'codecov/codecov-action@v4' .github/workflows/test.yml && grep -q 'secrets.CODECOV_TOKEN' .github/workflows/test.yml && grep -q 'lcov.info' .github/workflows/test.yml</automated>
    <manual>Verify Codecov action uses v4, token secret, and correct coverage file path</manual>
  </verify>
  <done>test.yml has correct Codecov upload configuration with v4 action and token secret</done>
</task>

</tasks>

<verification>
1. README.md contains Codecov badge with correct URL format
2. README.md contains NPM badge with correct shields.io URL
3. test.yml uses codecov/codecov-action@v4
4. test.yml references CODECOV_TOKEN secret
5. Coverage file path matches jest.config.ts output (coverage/lcov.info)
</verification>

<success_criteria>
- Badge URLs verified/corrected in README.md
- Codecov action v4 configured with token secret
- Coverage uploads to Codecov on push/PR (requires user to add CODECOV_TOKEN secret)
</success_criteria>

<output>
After completion, create `.planning/phases/20-codecov-badges/20-01-SUMMARY.md`
</output>
