---
phase: 18-typedoc-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/opencarl/types.ts
  - src/opencarl/loader.ts
  - src/opencarl/matcher.ts
  - src/opencarl/injector.ts
  - src/opencarl/signal-store.ts
  - src/opencarl/errors.ts
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Documentation shows organized categories instead of 'Other'"
    - "Each exported type/function has a meaningful category assignment"
    - "Categories reflect API consumer perspective (Configuration, Loading, Matching, etc.)"
    - "Category order in sidebar matches categoryOrder in typedoc.json"
  artifacts:
    - path: "src/opencarl/types.ts"
      provides: "Type definitions with @category tags"
      contains: "@category"
    - path: "src/opencarl/loader.ts"
      provides: "Loading functions with @category tags"
      contains: "@category"
    - path: "src/opencarl/matcher.ts"
      provides: "Matching functions with @category tags"
      contains: "@category"
    - path: "src/opencarl/injector.ts"
      provides: "Injection functions with @category tags"
      contains: "@category"
    - path: "src/opencarl/signal-store.ts"
      provides: "Signal functions with @category tags"
      contains: "@category"
    - path: "src/opencarl/errors.ts"
      provides: "Error types with @category tags"
      contains: "@category"
  key_links:
    - from: "src/opencarl/*.ts"
      to: "typedoc.json categoryOrder"
      via: "@category JSDoc tags"
      pattern: "@category\\s+(Configuration|Loading|Matching|Injection|Signals|Errors)"
---

<objective>
Add @category JSDoc tags to all exported TypeScript members for organized documentation navigation.

Purpose: Organize API documentation by functionality (not file structure) for intuitive developer experience.
Output: All exported types/functions tagged with appropriate @category.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-typedoc-setup/18-RESEARCH.md
@.planning/phases/18-typedoc-setup/18-CONTEXT.md
@.planning/phases/18-typedoc-setup/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add @category tags to core types (types.ts)</name>
  <files>src/opencarl/types.ts</files>
  <action>
Add @category JSDoc tags to all exported types in types.ts. These are foundational types used throughout OpenCARL.

Categories to assign:
- **Configuration**: OpencarlRuleSource, OpencarlRuleSourceScope, OpencarlRuleDiscoveryResult, OpencarlRuleDiscoveryWarning, OpencarlProjectStatus, OpencarlRuleDomainPayload
- **Matching**: OpencarlMatchRequest, OpencarlMatchResult, OpencarlMatchDomainConfig, OpencarlDomainMatchResult, OpencarlGlobalExcludeResult, OpencarlExclusionSource
- **Signals**: OpencarlSessionSignals, OpencarlSignalSource

Example format:
```typescript
/**
 * @category Configuration
 */
export interface OpencarlRuleSource {
  scope: OpencarlRuleSourceScope;
  path: string;
  domains: string[];
}

/**
 * @category Matching
 */
export interface OpencarlMatchRequest {
  promptText: string;
  signals: OpencarlSessionSignals;
  domains: Record<string, OpencarlMatchDomainConfig>;
  globalExclude: string[];
}

/**
 * @category Signals
 */
export interface OpencarlSessionSignals {
  promptTokens: string[];
  promptHistory: string[];
  toolTokens: string[];
  pathTokens: string[];
}
```

Ensure every exported type has a @category tag. No type should fall into "Other" category.
</action>
  <verify>
    <automated>grep -c "@category" src/opencarl/types.ts && npm run docs 2>&1 | grep -v "warning" | head -5</automated>
    <manual>Open docs/index.html and verify types appear under correct categories in sidebar</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>
    - All exported types in types.ts have @category tags
    - Running `npm run docs` shows types organized by category
    - No types appear in "Other" category (unless genuinely uncategorized)
  </done>
</task>

<task type="auto">
  <name>Add @category tags to loader functions</name>
  <files>src/opencarl/loader.ts</files>
  <action>
Add @category JSDoc tags to exported functions and interfaces in loader.ts.

Categories to assign:
- **Loading**: loadOpencarlRules (main function), OpencarlRuleDiscoveryOptions, OpencarlRuleDiscoveryOverrides

Example format:
```typescript
/**
 * @category Loading
 */
export interface OpencarlRuleDiscoveryOptions {
  cwd?: string;
  homeDir?: string;
  projectRoot?: string;
  overrides?: OpencarlRuleDiscoveryOverrides;
  projectOptIn?: boolean;
  sessionId?: string;
}

/**
 * Load OpenCARL rules from project, global, and fallback sources.
 * @category Loading
 */
export function loadOpencarlRules(
  options: OpencarlRuleDiscoveryOptions = {}
): OpencarlRuleDiscoveryResult {
  // ...
}
```

Add meaningful JSDoc descriptions where missing. Document parameters and return values.
</action>
  <verify>
    <automated>grep -c "@category" src/opencarl/loader.ts && test $(grep -c "@category" src/opencarl/loader.ts) -ge 2</automated>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>
    - loadOpencarlRules has @category Loading tag
    - OpencarlRuleDiscoveryOptions has @category Loading tag
    - OpencarlRuleDiscoveryOverrides has @category Loading tag
  </done>
</task>

<task type="auto">
  <name>Add @category tags to matcher, injector, signal-store, and errors</name>
  <files>src/opencarl/matcher.ts, src/opencarl/injector.ts, src/opencarl/signal-store.ts, src/opencarl/errors.ts</files>
  <action>
Add @category JSDoc tags to exported members in these files:

**matcher.ts** - Category: Matching
- matchDomainsForTurn

**injector.ts** - Category: Injection
- buildOpencarlInjection
- OpencarlInjectionInput

**signal-store.ts** - Category: Signals
- recordPromptSignals
- getSessionPromptText
- recordCommandSignals
- consumeCommandSignals
- recordToolSignals
- getSessionSignals

**errors.ts** - Category: Errors
- OpencarlError (class)
- OpencarlErrorCategory
- formatError
- manifestParseError
- domainLoadError
- permissionError
- configReadError
- setupError
- integrationError

Example format for matcher.ts:
```typescript
/**
 * Match domains against current turn signals.
 * @category Matching
 */
export function matchDomainsForTurn(request: OpencarlMatchRequest): OpencarlMatchResult {
```

Example format for errors.ts:
```typescript
/**
 * Structured error class for OpenCARL-specific errors.
 * @category Errors
 */
export class OpencarlError extends Error {
```

Add meaningful JSDoc descriptions where missing. Ensure all exports have categories.
</action>
  <verify>
    <automated>grep -c "@category" src/opencarl/matcher.ts && grep -c "@category" src/opencarl/injector.ts && grep -c "@category" src/opencarl/signal-store.ts && grep -c "@category" src/opencarl/errors.ts && npm run docs && test -f docs/index.html</automated>
    <manual>Open docs/index.html and verify sidebar shows all 6 categories with members</manual>
    <sampling_rate>run after task commits, before marking complete</sampling_rate>
  </verify>
  <done>
    - All exported functions in matcher.ts have @category Matching
    - All exported functions/types in injector.ts have @category Injection
    - All exported functions in signal-store.ts have @category Signals
    - All exported types/functions in errors.ts have @category Errors
    - Documentation shows all 6 categories populated in sidebar
  </done>
</task>

</tasks>

<verification>
1. Run `npm run docs` - should complete without errors
2. Check docs/index.html contains all category sections
3. Verify no exports appear in "Other" category (check for empty Other section)
4. Confirm sidebar shows: Configuration, Loading, Matching, Injection, Signals, Errors
</verification>

<success_criteria>
- All exported TypeScript members have @category tags
- Documentation displays 6 organized categories (no "Other" with significant content)
- Categories reflect API consumer perspective per CONTEXT.md decision
- `npm run docs` generates valid documentation with organized navigation
</success_criteria>

<output>
After completion, create `.planning/phases/18-typedoc-setup/18-02-SUMMARY.md`
</output>
