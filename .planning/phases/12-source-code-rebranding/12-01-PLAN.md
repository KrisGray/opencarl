---
phase: 12-source-code-rebranding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/carl/types.ts
  - src/carl/errors.ts
  - src/carl/loader.ts
  - src/carl/injector.ts
  - src/carl/matcher.ts
  - src/carl/session-overrides.ts
  - src/carl/signal-store.ts
  - tests/javascript/unit/validate.test.ts
  - tests/javascript/unit/injector.test.ts
  - tests/javascript/unit/matcher.test.ts
  - tests/javascript/unit/command-parity.test.ts
  - tests/javascript/unit/rule-cache.test.ts
  - tests/javascript/integration/plugin-lifecycle.test.ts
  - tests/javascript/integration/rule-injection.test.ts
autonomous: true
requirements:
  - SOURCE-01

must_haves:
  truths:
    - All TypeScript types use "Opencarl" prefix instead of "Carl"
    - Type declarations in src/carl/ use new naming convention
    - Type references in test files use new naming convention
  artifacts:
    - path: "src/carl/types.ts"
      provides: "Type definitions with Opencarl prefix"
      contains: "OpencarlRuleSourceScope|OpencarlRuleSource|OpencarlSessionSignals|OpencarlMatchRequest"
    - path: "src/carl/errors.ts"
      provides: "Error types with Opencarl prefix"
      contains: "OpencarlErrorCategory|OpencarlError"
    - path: "src/carl/injector.ts"
      provides: "Injector types with Opencarl prefix"
      contains: "OpencarlInjectionInput|OpencarlInjectionOutput"
    - path: "src/carl/matcher.ts"
      provides: "Matcher types with Opencarl prefix"
      contains: "OpencarlMatchDomainConfig|OpencarlMatchResult"
    - path: "tests/javascript/unit/validate.test.ts"
      provides: "Test imports with Opencarl types"
      contains: "import.*OpencarlRuleDiscoveryWarning"
  key_links:
    - from: "src/carl/types.ts"
      to: "src/carl/loader.ts|src/carl/injector.ts|src/carl/matcher.ts"
      via: "type exports"
      pattern: "export (type|interface) Opencarl"
    - from: "tests/javascript/**/*.test.ts"
      to: "src/carl/types.ts"
      via: "import statements"
      pattern: "import.*Opencarl.*from.*types"
---

<objective>
Rename all TypeScript type names with "Carl" prefix to "Opencarl" prefix across source and test files

Purpose: Establish the foundational type naming convention for the rebranding effort
Output: Updated type declarations and references using Opencarl prefix
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-source-code-rebranding/12-CONTEXT.md
@.planning/phases/12-source-code-rebranding/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename type declarations in source files</name>
  <files>
    src/carl/types.ts
    src/carl/errors.ts
    src/carl/loader.ts
    src/carl/injector.ts
    src/carl/matcher.ts
    src/carl/session-overrides.ts
    src/carl/signal-store.ts
  </files>
  <action>
    Update all TypeScript type/interface/class declarations with "Carl" prefix to "Opencarl" prefix:
    - Replace "CarlRuleSourceScope" with "OpencarlRuleSourceScope"
    - Replace "CarlRuleSource" with "OpencarlRuleSource"
    - Replace "CarlRuleDiscoveryWarning" with "OpencarlRuleDiscoveryWarning"
    - Replace "CarlProjectStatus" with "OpencarlProjectStatus"
    - Replace "CarlRuleDiscoveryResult" with "OpencarlRuleDiscoveryResult"
    - Replace "CarlRuleDomainPayload" with "OpencarlRuleDomainPayload"
    - Replace "CarlSignalSource" with "OpencarlSignalSource"
    - Replace "CarlSessionSignals" with "OpencarlSessionSignals"
    - Replace "CarlMatchDomainConfig" with "OpencarlMatchDomainConfig"
    - Replace "CarlMatchRequest" with "OpencarlMatchRequest"
    - Replace "CarlExclusionSource" with "OpencarlExclusionSource"
    - Replace "CarlDomainMatchResult" with "OpencarlDomainMatchResult"
    - Replace "CarlGlobalExcludeResult" with "OpencarlGlobalExcludeResult"
    - Replace "CarlMatchResult" with "OpencarlMatchResult"
    - Replace "CarlInjectionInput" with "OpencarlInjectionInput"
    - Replace "CarlInjectionOutput" with "OpencarlInjectionOutput"
    - Replace "CarlErrorCategory" with "OpencarlErrorCategory"
    - Replace "CarlError" with "OpencarlError"

    Follow the locked decision from CONTEXT.md:
    - Use "Opencarl" prefix (CamelCase, lowercase "carl")
    - Compound names: "CarlRuleDomainPayload" → "OpencarlRuleDomainPayload" (lowercase "c" after prefix)
    - Multiple "Carl" occurrences: Replace only the prefix

    Update both the type declarations and all references within the same files.
  </action>
  <verify>
    npx tsc --noEmit
  </verify>
  <done>
    All type declarations in src/carl/ use "Opencarl" prefix, and TypeScript compilation succeeds
  </done>
</task>

<task type="auto">
  <name>task 2: Update type references in test files</name>
  <files>
    tests/javascript/unit/validate.test.ts
    tests/javascript/unit/injector.test.ts
    tests/javascript/unit/matcher.test.ts
    tests/javascript/unit/command-parity.test.ts
    tests/javascript/unit/rule-cache.test.ts
    tests/javascript/integration/plugin-lifecycle.test.ts
    tests/javascript/integration/rule-injection.test.ts
  </files>
  <action>
    Update all import statements and type references in test files to use "Opencarl" prefix:
    - Replace import statements referencing Carl types with Opencarl types
    - Replace type annotations using Carl types with Opencarl types
    - Update typeof imports for dynamic module loading

    Examples:
    - "import type { CarlRuleDiscoveryWarning } from" → "import type { OpencarlRuleDiscoveryWarning } from"
    - "import type { CarlRuleDomainPayload } from" → "import type { OpencarlRuleDomainPayload } from"
    - "import type { CarlInjectionInput } from" → "import type { OpencarlInjectionInput } from"
    - "typeof import('../../../src/carl/loader')" → "typeof import('../../../src/carl/loader')" (no change, but internal types update automatically)
  </action>
  <verify>
    npm test -- --testPathPattern="validate.test|injector.test|matcher.test|command-parity.test|rule-cache.test|plugin-lifecycle.test|rule-injection.test"
  </verify>
  <done>
    All test files import and use Opencarl types correctly, and tests pass
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation succeeds: npx tsc --noEmit
2. All affected tests pass: npm test -- --testPathPattern="validate|injector|matcher|command-parity|rule-cache|plugin-lifecycle|rule-injection"
3. No remaining "Carl" type references in source or test files (grep -r "interface Carl\|type Carl\|class Carl" src/carl tests/javascript)
</verification>

<success_criteria>
All TypeScript type names use "Opencarl" prefix, compilation succeeds, and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-code-rebranding/12-01-SUMMARY.md`
</output>
