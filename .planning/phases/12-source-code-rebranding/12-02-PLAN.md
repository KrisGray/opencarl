---
phase: 12-source-code-rebranding
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/carl/loader.ts
  - src/carl/setup.ts
  - src/carl/rule-cache.ts
  - src/carl/debug.ts
  - src/carl/command-parity.ts
  - tests/javascript/unit/loader.test.ts
  - tests/javascript/unit/setup.test.ts
  - tests/javascript/unit/debug.test.ts
  - tests/javascript/integration/setup-and-domain-workflow.test.ts
  - tests/javascript/integration/file-system-operations.test.ts
  - tests/javascript/e2e/setup-flow.test.ts
  - tests/javascript/e2e/keyword-matching.test.ts
  - tests/javascript/e2e/star-commands.test.ts
autonomous: true
requirements:
  - SOURCE-02

must_haves:
  truths:
    - All function names use "opencarl" prefix instead of "carl"
    - All variable names use "opencarl" prefix instead of "carl"
    - Function/variable naming follows existing patterns (snake_case → opencarl_xxx, camelCase → opencarlXxx)
  artifacts:
    - path: "src/carl/loader.ts"
      provides: "Loader function with opencarl prefix"
      contains: "loadOpencarlRules|resolveSourceFromOverride"
    - path: "src/carl/setup.ts"
      provides: "Setup functions with opencarl prefix"
      contains: "setupOpencarl|integrateOpencarl"
    - path: "src/carl/debug.ts"
      provides: "Debug functions with opencarl prefix"
      contains: "enableOpencarlDebug|disableOpencarlDebug"
  key_links:
    - from: "src/carl/loader.ts"
      to: "tests/javascript/unit/loader.test.ts"
      via: "function exports and imports"
      pattern: "export function loadOpencarlRules|import.*loadOpencarlRules"
    - from: "src/carl/setup.ts"
      to: "tests/javascript/integration/setup-and-domain-workflow.test.ts"
      via: "function exports"
      pattern: "setupOpencarl"
---

<objective>
Rename all function and variable names with "carl" prefix to "opencarl" prefix across source and test files

Purpose: Establish consistent function and variable naming convention aligned with type rebranding
Output: Updated function/variable declarations and references using opencarl prefix
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-source-code-rebranding/12-CONTEXT.md
@.planning/phases/12-source-code-rebranding/12-RESEARCH.md
@.planning/phases/12-source-code-rebranding/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename function and variable declarations in source files</name>
  <files>
    src/carl/loader.ts
    src/carl/setup.ts
    src/carl/rule-cache.ts
    src/carl/debug.ts
    src/carl/command-parity.ts
  </files>
  <action>
    Update all function and variable declarations with "carl" prefix to "opencarl" prefix:

    Function names (camelCase):
    - "loadCarlRules" → "loadOpencarlRules"
    - "buildCarlHelpGuidance" → "buildOpencarlHelpGuidance"
    - "setupCarl" → "setupOpencarl"
    - "integrateCarl" → "integrateOpencarl"
    - "removeCarlIntegration" → "removeOpencarlIntegration"
    - "buildCarlInjection" → "buildOpencarlInjection"
    - "resolveCarlCommandSignals" → "resolveOpencarlCommandSignals"
    - "enableCarlDebug" → "enableOpencarlDebug"
    - "disableCarlDebug" → "disableOpencarlDebug"
    - "isCarlDebugEnabled" → "isOpencarlDebugEnabled"

    Variable names (camelCase):
    - "carlDir" → "opencarlDir" (local variables, function parameters)
    - "carlDocsPath" → "opencarlDocsPath"
    - "projectCarl" → "projectOpencarl"
    - "globalCarl" → "globalOpencarl"

    Follow the locked decision from CONTEXT.md:
    - Rename ALL functions and variables (not just internal ones)
    - No backward compatibility aliases — clean break
    - Follow existing patterns: snake_case "carl_xxx" → "opencarl_xxx", camelCase "carlXxx" → "opencarlXxx"
    - Private members rename fully: "_carlXxx" → "_opencarlXxx", "#carlXxx" → "#opencarlXxx"

    Update both the declarations and all references within the same files.
  </action>
  <verify>
    npx tsc --noEmit
  </verify>
  <done>
    All function/variable declarations in src/carl/ use "opencarl" prefix, and TypeScript compilation succeeds
  </done>
</task>

<task type="auto">
  <name>task 2: Update function/variable references in test files</name>
  <files>
    tests/javascript/unit/loader.test.ts
    tests/javascript/unit/setup.test.ts
    tests/javascript/unit/debug.test.ts
    tests/javascript/integration/setup-and-domain-workflow.test.ts
    tests/javascript/integration/file-system-operations.test.ts
    tests/javascript/e2e/setup-flow.test.ts
    tests/javascript/e2e/keyword-matching.test.ts
    tests/javascript/e2e/star-commands.test.ts
  </files>
  <action>
    Update all function imports and variable references in test files to use "opencarl" prefix:
    - Replace import statements referencing Carl functions with Opencarl functions
    - Replace function calls using old names with new names
    - Update variable names in test fixtures and expectations
    - Update assertions that reference old function names

    Examples:
    - "import { loadCarlRules } from" → "import { loadOpencarlRules } from"
    - "loadCarlRules(" → "loadOpencarlRules("
    - "setupCarl(" → "setupOpencarl("
    - "buildCarlInjection(" → "buildOpencarlInjection("
    - Variable references: "carlDir" → "opencarlDir", "carlDocsPath" → "opencarlDocsPath"

    Also update fixture files if they contain variable names with carl prefix.
  </action>
  <verify>
    npm test -- --testPathPattern="loader|setup|debug|setup-and-domain-workflow|file-system-operations|setup-flow|keyword-matching|star-commands"
  </verify>
  <done>
    All test files import and call functions with "opencarl" prefix correctly, and tests pass
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation succeeds: npx tsc --noEmit
2. All affected tests pass: npm test -- --testPathPattern="loader|setup|debug|setup-and-domain-workflow|file-system-operations|setup-flow|keyword-matching|star-commands"
3. No remaining "carl" function/variable prefixes in declarations (grep -r "function carl\|const carl\|let carl\|var carl" src/carl --include="*.ts")
</verification>

<success_criteria>
All function and variable names use "opencarl" prefix, compilation succeeds, and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-code-rebranding/12-02-SUMMARY.md`
</output>
