---
phase: 06-integration-developer-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/carl/setup.ts
  - src/integration/opencode-config.ts
  - src/integration/plugin-hooks.ts
autonomous: true
requirements:
  - INTE-02
must_haves:
  truths:
    - "User can run /carl setup --integrate-opencode to add CARL docs to opencode.json"
    - "CARL instructions are merged with existing opencode.json instructions, not replaced"
    - "opencode.json is created with minimal CARL-focused config if file doesn't exist"
    - "Integration is opt-in via flag, never automatic"
  artifacts:
    - path: "src/integration/opencode-config.ts"
      provides: "opencode.json read/write/merge functions"
      exports: ["readOpenCodeConfig", "mergeCarlInstructions", "writeOpenCodeConfig"]
    - path: "src/carl/setup.ts"
      provides: "--integrate-opencode flag handling"
      contains: "integrateOpencode"
  key_links:
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/setup.ts"
      via: "command.execute.before hook"
      pattern: "integrate-opencode"
---

<objective>
Add `--integrate-opencode` flag to the setup command that merges CARL documentation references into the user's opencode.json instructions field.

Purpose: Enable users to make CARL docs available to OpenCode's instruction system without manual config editing.
Output: Working `/carl setup --integrate-opencode` command that intelligently merges CARL instructions.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/carl/setup.ts
@src/integration/plugin-hooks.ts
@resources/docs/CARL-DOCS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create opencode.json config utilities</name>
  <files>src/integration/opencode-config.ts</files>
  <action>
Create a new module for opencode.json config management:

1. Define `OpenCodeConfig` interface with `instructions` field as string or string array
2. Implement `readOpenCodeConfig(cwd: string)`:
   - Read opencode.json from cwd
   - Return default empty config `{ instructions: [] }` if file doesn't exist
   - Parse JSON and validate structure (catch malformed JSON with clear error)
3. Implement `mergeCarlInstructions(config: OpenCodeConfig, carlDocPaths: string[]): OpenCodeConfig`:
   - Convert existing instructions to array if it's a string
   - Check if CARL docs already referenced (idempotent - don't duplicate)
   - Add CARL doc paths to instructions array
   - Preserve all existing instructions
   - CARL paths to add: `./resources/docs/CARL-DOCS.md` (or appropriate relative path)
4. Implement `writeOpenCodeConfig(cwd: string, config: OpenCodeConfig)`:
   - Write JSON with 2-space indentation
   - Create file if doesn't exist
   - Handle write errors gracefully

Reference the existing agents-writer.ts pattern for marker-based idempotency.
  </action>
  <verify>
    <automated>node -e "const m = require('./dist/integration/opencode-config.js'); console.log(typeof m.readOpenCodeConfig, typeof m.mergeCarlInstructions, typeof m.writeOpenCodeConfig)"</automated>
  </verify>
  <done>Module exports readOpenCodeConfig, mergeCarlInstructions, writeOpenCodeConfig functions</done>
</task>

<task type="auto">
  <name>Task 2: Add --integrate-opencode flag to setup</name>
  <files>src/carl/setup.ts, src/integration/plugin-hooks.ts</files>
  <action>
Extend the setup system with opencode.json integration:

1. In setup.ts, add `integrateOpencode()` function:
   - Call readOpenCodeConfig(cwd)
   - Call mergeCarlInstructions() with path to CARL-DOCS.md
   - Call writeOpenCodeConfig(cwd, mergedConfig)
   - Return IntegrationResult with success/message
   - Handle all errors with actionable messages

2. In plugin-hooks.ts, add command handler for `/carl setup --integrate-opencode`:
   - Parse command: detect "integrate-opencode" in command string
   - Call integrateOpencode({ cwd: process.cwd() })
   - Console.log the result message
   - Pattern matches existing --integrate and --remove handlers

3. Update IntegrationOptions interface to include `integrateOpencode?: boolean`

Follow the existing pattern from runIntegration() for AGENTS.md integration.
  </action>
  <verify>
    <automated>npm run build && echo "Build successful"</automated>
  </verify>
  <done>/carl setup --integrate-opencode command works and updates opencode.json</done>
</task>

</tasks>

<verification>
1. Run `/carl setup --integrate-opencode` in a test directory with no opencode.json - verify file created with CARL docs reference
2. Run again on same directory - verify idempotent (no duplicate entries)
3. Run on directory with existing opencode.json instructions - verify CARL added without replacing existing
</verification>

<success_criteria>
- `/carl setup --integrate-opencode` creates/updates opencode.json
- Existing instructions are preserved
- Running twice doesn't create duplicates
- Clear success/error messages displayed
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-developer-experience/06-01-SUMMARY.md`
</output>
