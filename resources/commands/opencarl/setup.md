---
name: setup
description: Initialize OpenCARL. This command is handled by the plugin - no action needed.
---

# /opencarl setup - Plugin Handled

**IMPORTANT: This command is executed by the OpenCARL plugin hook. Do NOT manually create any files.**

The plugin automatically:
1. Seeds `.opencarl/` templates from `.opencarl-template/`
2. Copies commands/skills to `~/.opencode/`

Simply acknowledge: "OpenCARL setup is handled by the plugin. Check the console output for results."
